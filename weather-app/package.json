{
  "name": "weather-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",       // Render runs this
    "dev": "nodemon server.js"       // Optional (for local dev)
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.3"   // Optional (security)
  },
  "engines": {
    "node": ">=18.x"                // Specify Node version
  }
}
